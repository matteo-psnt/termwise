# Only the rendered GIFs the README embeds belong here. The recording scripts,
# the intermediate casts and the MP4s live outside the published tree, in
# docs/demo-recording/ — see its README for how to regenerate these.
*
!.gitignore
!*.gif
